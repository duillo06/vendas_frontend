# Build de produção — variáveis injetadas no CI/CD ou Docker build args
# /api/v1 = mesma origem (Nginx no tenant/admin) — sem CORS no browser

VITE_API_BASE_URL=/api/v1
VITE_API_PORT=443
VITE_DEFAULT_TENANT_SUBDOMAIN=demo
VITE_STOREFRONT_BASE_DOMAIN=pediu.cloud
